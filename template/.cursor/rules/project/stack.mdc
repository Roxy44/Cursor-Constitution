---
description: Project facts — stack, tooling, conventions, communication language, docs locale. Generated by the onboarding interview.
alwaysApply: true
---

# This project

Filled by `onboarding.mdc`. Keep it under ~30 lines: facts only, no explanations. If a
value still says `TBD`, run the onboarding interview before deep work (**M-1**).

Rule `.mdc` files stay English. Everything below about "language" is for humans and memory.

- **Communication language:** TBD
- **Docs locale:** TBD (`en` | `ru` — which pack was unpacked into flat `docs/`)
- **UI / product copy:** TBD (same as communication / en-only / i18n keys / no rule)
- **Project type:** TBD (frontend / backend / fullstack / library / CLI / mobile)
- **Languages:** TBD (programming languages, e.g. TypeScript — not dialogue language)
- **Frontend:** TBD (framework, styling, state, data layer)
- **Backend:** TBD (runtime, framework, database, ORM)
- **CSS naming:** TBD (bem / existing / none / n/a — ask only for classic CSS/SCSS/Modules; n/a for Tailwind & CSS-in-JS)
- **Package manager:** TBD (e.g. npm / pnpm / yarn / bun — agent MUST use this for installs)
- **Lockfiles in git:** TBD (ignore — default greenfield / commit — if team wants pinned CI)
- **Lint / format:** TBD (e.g. Biome / ESLint+Prettier / Ruff / none — not assumed)
- **Tests:** TBD (runner, command, what is expected)
- **Verify command(s):** TBD
- **Branching / commits:** TBD (agent never commits or pushes — **G-1**, **G-5**)
- **Hard constraints:** TBD

## Layout

- Frontend code: TBD
- Backend code: TBD
- Tests: TBD
- Docs root: docs
